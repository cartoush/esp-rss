export component HelloWorld inherits Rectangle {
    width: 320px;
    height: 240px;
    background: #a16277;

    Text {
       y: parent.height / 2;
       x: parent.width / 2;
       text: "Hello, world";
       color: red;
    }
}